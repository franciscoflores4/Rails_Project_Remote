	<div class="container">
	<h1>Jerseys</h1>
	<%= paginate @jerseys%>
<% @jerseys.each do |jersey| %>
  <div class="row">
    <div class="col-xs-3">
	<p><%= link_to image_tag(jersey.image), jersey_path(jersey) %></p>
	
	
		<h3><%= jersey.player[:name] %></h3>
		<h3><%= jersey.team[:name] %></h3>
		<h3><%= jersey.size %></h3>
		<h2><%= jersey.price %></h2>
		<%= button_to 'Add to Cart', carts_show_path, method: :create %>
		</div>
		 <% end %>
  </div>

</div>
